log syslog all;
debug protocols all;


ipv4 table bgpannounce;

function safe_export() {
        if source = RTS_STATIC then {return false;}
        if net ~ [172.31.16.0/20, 172.17.0.0/16, $INTERNAL_PREFIX] then {return false;}
        return true;
}

protocol device {
        scan time 10;
}

protocol direct {
        interface "*";
        ipv4 {
            table bgpannounce;
        };
}

protocol kernel {
        kernel table $KERNEL_TABLE_NUMBER;
        ipv4 {
            table bgpannounce;
            import filter {
               if source = RTS_BGP then {accept;}
               reject;
            };
        };
        scan time 10;
        learn off;
        persist off;
        graceful restart on;
        #device routes off;
}

protocol mrt {
        table "bgpannounce";
        filename "/tmp/bird_log_mrt/bgpannounce.mrt";
        period 5;
}                           


